rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow anyone to read files (e.g., view images on the public site)
      allow read;
      // Only allow authenticated users to write (upload, update, delete)
      allow write: if request.auth != null;
    }
  }
}
